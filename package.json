{
  "name": "frappe_tools",
  "version": "1.0.0",
  "description": "Frappe Custom Tools App",
  "scripts": {
    "build": "yarn --cwd ocr_ui install --frozen-lockfile --non-interactive && yarn --cwd ocr_ui build"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "uuid": "^13.0.0",
    "pinia": "^3.0.4",
    "pinia-plugin-persistedstate": "^4.7.1"
  },
  "optionalDependencies": {
    "@openai/codex": "0.152.1"
  }
}
